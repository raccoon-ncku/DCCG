"""Not the entry point for anything.

This file is left over from `uv init`. The course has no single program to run --
each lecture, exercise and checkpoint is its own script.

If you are looking for where to start:

    uv run check.py         # how far you are in every week
    uv run check.py 01      # work on Week 01

And read the week's lecture README, linked from the schedule in README.md.
"""


def main():
    print(__doc__)


if __name__ == "__main__":
    main()
